package com.work.taskCatalog.mapper

import com.work.taskCatalog.dto.TaskCreateDto
import com.work.taskCatalog.model.Task

class TaskMapper {
    companion object {

        fun toTask( createDto: TaskCreateDto) {
        }
    }
}